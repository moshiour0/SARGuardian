# Dependencies, pinned.
#
# The lower bounds this file used to carry did not describe an environment, they
# described a hope. A referee reproducing a figure in a year needs the versions
# the numbers were computed with, and "numpy>=1.24" is not that. These are the
# versions every result in the README was produced under.
#
# For a permissive install (teammate, CI on a different platform), the minimum
# workable versions are noted alongside each pin.

# Core — required
numpy==2.4.6            # min 1.24
h5py==3.16.0            # min 3.9

# Download (search works without it, stdlib only)
asf_search==13.0.0      # min 7.0

# GeoTIFF export and reprojection
rasterio==1.4.4         # min 1.3
pyproj==3.7.2           # min 3.6

# Quicklooks and plots
matplotlib==3.11.1      # min 3.7

# Tests
pytest==9.1.1           # min 7.0

# Optional — notebooks. Not pinned because nothing in the results depends on it.
# jupyterlab>=4.0
